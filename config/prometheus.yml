global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node_exporter'
    static_configs:
      # Merk: Siden node-exporter kjører i container-nettverket
      # bruker vi servicenavnet som DNS.
      - targets: ['node-exporter:9100']
